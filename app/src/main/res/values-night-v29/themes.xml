<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PCSimulatorSaveEditor" parent="Theme.MaterialComponents.DayNight">
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:dialogTheme">@style/Theme.MaterialComponents.DayNight.Dialog</item>
    </style>

    <style name="Theme.PCSimulatorSaveEditor.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:dialogTheme">@style/Theme.MaterialComponents.DayNight.Dialog</item>
    </style>
    <!-- Redirect to the theme above to prevent crashes -->
    <style name="Theme.PCSimulatorSaveEditor.Splash" parent="Theme.PCSimulatorSaveEditor"/>
</resources>